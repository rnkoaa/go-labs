# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  default:
    deps: ["build"]
    cmds:
      - ./cert-manager serve
    silent: true
  build:
    cmds:
      - go build -o cert-manager ./cmd/cert-manager/
    silent: true
